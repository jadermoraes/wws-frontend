import { Component, OnDestroy, OnInit } from '@angular/core';

@Component({
  selector: 'at-homepage',
  templateUrl: 'homepage.component.html',
  styleUrls: ['./homepage.component.scss'],
})

export class HomePageComponent implements OnInit, OnDestroy {

  constructor(
  ) { }

  ngOnInit() {
  }

  ngOnDestroy() {
  }
}
